# Use the SESSION POOLER url from Supabase → Settings → Database → Connection pooling
# Format: postgresql://postgres.[project-ref]:[PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:5432/postgres
# The direct connection (db.[project-ref].supabase.co) does not resolve on this machine
DATABASE_URL=
JWT_SECRET=
FRONTEND_URL=
PORT=3000